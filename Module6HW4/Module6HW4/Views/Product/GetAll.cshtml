@model IEnumerable<Module6HW4.core.Entity.Product>

@{
	ViewData["Title"] = "GetAll";
}

<h1>GetAll</h1>

<p>
	<a asp-action="PostPage">Create New</a>
</p>
<table class="table">
	<thead>
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Id)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Name)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Price)
			</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.DisplayFor(modelItem => item.Id)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Name)
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.Price)
				</td>
				<td>
					@Html.ActionLink("Edit", "EditPage", new { /* id=item.PrimaryKey */ }) |
					@Html.ActionLink("Details", "GetEntityById", new {  id=item.Id }) |
					@Html.ActionLink("Delete", "DeletePage", new {  id=item.Id }) |
				</td>
			</tr>
		}
	</tbody>
</table>
